---
import BaseLayout from "../layouts/BaseLayout.astro";
import "../styles/global.css";
---

<BaseLayout title="Friends of Lake Henry">
	<div class="container">
		<section class="hero card enter-block delay-1">
			<div class="pad">
				<h1>Protecting and restoring Lake Henry—together.</h1>
				<p>
					Friends of Lake Henry is a community nonprofit raising funds and organizing
					support for long-term lake restoration.
				</p>
			</div>
		</section>

		<section class="highlights enter-block delay-2">
			<div class="highlightGrid">
				<a class="card highlight" href="/donate">
					<h2>Donate</h2>
					<p class="muted">Support lake restoration efforts with a one-time or recurring gift.</p>
				</a>

				<a class="card highlight" href="/raffle">
					<h2>Raffle</h2>
					<p class="muted">See this month’s calendar raffle and prize schedule.</p>
				</a>

				<a class="card highlight" href="/events">
					<h2>Events</h2>
					<p class="muted">View upcoming fundraisers and community updates.</p>
				</a>
			</div>
		</section>

		<section class="contactSection card enter-block delay-3">
			<div class="pad">
				<h2>Contact</h2>
				<p>
					Have a question, want to volunteer, or need raffle information? Send us a
					message and we’ll get back to you.
				</p>

				<form class="form" method="post" action="/api/contact">
					<div class="row">
						<label>
							Name
							<input name="name" autocomplete="name" required />
						</label>
						<label>
							Email
							<input name="email" type="email" autocomplete="email" required />
						</label>
					</div>

					<label>
						Message
						<textarea name="message" rows="6" required></textarea>
					</label>

					<button class="btn" type="submit">Send message</button>

					<p class="fine muted">
						By submitting, you agree we may contact you back about your request.
					</p>
				</form>
			</div>
		</section>
	</div>

	<style>
		.hero{ margin-top: 1.25rem; }
		.contactSection{ margin-top: 1.5rem; margin-bottom: 2.5rem; }

		.pad { padding: 2rem; }
		h1 { margin: 0 0 0.75rem; font-size: 2rem; }
		.hero p,
		.contactSection p {
			margin: 0 0 1.25rem;
			max-width: 65ch;
			line-height: 1.6;
			color: var(--text-strong);
		}
		.hero p { margin-bottom: 0; }
		p.fine { margin-top: 0.75rem; }
		p.fine.muted { color: var(--text-soft); }

		h2 { margin: 0 0 0.75rem; font-size: 1.4rem; }
		.muted { color: var(--muted); }
		.fine { font-size: 0.95rem; margin-top: 0.75rem; }

		.form { margin-top: 1rem; display: grid; gap: 1rem; }
		.row { display: grid; gap: 1rem; grid-template-columns: 1fr; }
		label { display: grid; gap: 0.4rem; color: var(--text); font-weight: 700; }

		input, textarea{
			padding: 0.75rem 0.85rem;
			border-radius: 12px;
			border: 1px solid rgba(255,255,255,0.10);
			background: rgba(0,0,0,0.12);
			color: var(--text);
		}
		textarea { resize: vertical; }
		.highlights{ margin-top: 1.75rem; }

		.highlightGrid{
			display: grid;
			gap: 1rem;
			grid-template-columns: 1fr;
		}

		.highlight{
			padding: 1.5rem;
			text-decoration: none;
			transition: transform 140ms ease, border-color 160ms ease, background-color 160ms ease;
		}

		.highlight h2{
			margin: 0 0 .5rem;
			font-size: 1.2rem;
		}

		.highlight p{
			margin: 0;
		}

		.highlight:hover,
		.highlight:focus-visible{
			transform: translateY(-2px);
			border-color: rgba(255,255,255,0.22);
		}

		@media (min-width: 860px){
			.row { grid-template-columns: 1fr 1fr; }
			.highlightGrid{ grid-template-columns: repeat(3, 1fr); }
		}
	</style>
</BaseLayout>
